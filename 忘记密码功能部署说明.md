# 忘记密码功能 · 部署与使用说明

本次新增「忘记密码」自助找回能力（零付费方案：手机号确认身份 + 密保问题 / 恢复码重置）。
手机号的角色是**身份确认展示**（脱敏显示），真正的验证靠密保答案或恢复码——纯前端即可完成，无需短信服务。

---

## 一、本地模式（双击离线版）—— 开箱即用，无需任何部署

本地版数据存在本机浏览器 IndexedDB，前端可直接读写，**改完即用**，无需额外配置。

功能点：
- 注册成功后弹出**一次性恢复码**，请截图 / 抄写保存（关闭后不可再查看）。
- 「切换 / 管理账号 → 修改账号信息」里可补充：手机号、密保问题 + 答案、随时重新生成恢复码。
- 登录界面点「忘记密码？」→ 输入用户名 → 显示脱敏手机号 + 密保问题 → 答密保 **或** 输恢复码 → 设置新密码。

---

## 二、云端模式（Supabase 线上版）—— 需完成 2 步后台配置

云端版因 Supabase 安全限制（未登录无法改他人密码），新增的两步配置需要你在 Supabase 后台操作一次。

### 步骤 1：执行补充 SQL（加字段 + 查询函数）

1. 打开 Supabase 后台 → 左侧 **SQL Editor** → **New query**
2. 打开本目录的 `supabase_forgot_pwd.sql`，**全选复制**粘贴进去
3. 点 **Run**
4. 看到 `Success. No rows returned` 即成功

> 该 SQL 只给 `profiles` 表加 3 个字段，并创建一个供未登录查询的 RPC，不影响已有数据。

### 步骤 2：部署 Edge Function（服务端改密）

1. Supabase 后台 → 左侧 **Functions** → 右上角 **New function**
2. 名称填：`reset-pwd`（必须完全一致）
3. 把本目录 `supabase/functions/reset-pwd/index.ts` 的内容**全选复制**粘贴进编辑器
4. 展开函数设置，添加两个 **Secrets（环境变量）**：
   - `SUPABASE_URL` = 你的项目 URL（格式 `https://xxxx.supabase.co`）
   - `SUPABASE_SERVICE_ROLE_KEY` = 项目 **Settings → API** 里的 `service_role` 密钥（**仅在后台填写，不会出现在前端**）
5. 点 **Deploy**（或 Save + Deploy）

> 部署成功后，云端「忘记密码」即可通过该函数校验密保 / 恢复码并重置密码。
> 未部署函数时，云端忘记密码会提示「重置失败」——属正常，部署后生效。

---

## 三、重新发布前端

把最新的 `五金厂管理系统.html` 重新上传到 GitHub（与之前同样的操作）：
`Add file → Upload files` → 覆盖 `Hardware Factory Management System.html` → 等 1~2 分钟 → 强刷浏览器。

---

## 四、使用建议

1. **注册后务必保存恢复码**（弹窗关闭后不可再查看）。
2. 建议到「修改账号信息」补全**手机号 + 密保问题**，双重保险。
3. 云端版在部署 Edge Function **之前**已注册的旧账号，`profiles` 里恢复码为空，请登录后到「修改账号信息」点「生成新恢复码」补齐。
4. 密保答案、恢复码只在前端做哈希存储，服务端不存明文；但请妥善保管，泄露等同于给他人重置密码的权限。

---

## 五、文件清单

| 文件 | 用途 |
|------|------|
| `supabase_forgot_pwd.sql` | 云端补充 SQL（加字段 + 查询 RPC），步骤 1 使用 |
| `supabase/functions/reset-pwd/index.ts` | Edge Function 源码，步骤 2 粘贴部署 |
| `忘记密码功能部署说明.md` | 本说明 |
